!!!xdrip "<img src="../../images/hamburger_menu.png" style="zoom:75%;" />"  
    &ensp;Settings  
    &emsp;Hardware Data Source  
    &ensp;&emsp;Dex

### G6 or Dexcom ONE

This describes how to set up a Dexcom G6 or Dexcom ONE transmitter and sensor with xDrip. From xDrip's perspective the two are the same device — same transmitter hardware, same BLE protocol — so every step below applies equally to both.

Reference documentation is [here](https://navid200.github.io/xDrip/docs/Dexcom_page.html).

For G7 or ONE+, see the [separate G7, ONE+ page](../g7) instead.

#### Prerequisites

- Check your phone setup matches xDrip [prerequisites](./prerequisites.md).
- You cannot normally have both xDrip and the Dexcom app collecting directly from the same transmitter at the same time. Uninstall the Dexcom app if you want xDrip to be the collector. If you need to keep using the Dexcom app, only it can be the collector — set up xDrip as a [Companion app](../companion) follower instead.

!!!warning "Don't pair from Android Bluetooth settings"  
    Never try to pair the transmitter from Android Bluetooth settings page — let xDrip trigger the pairing request instead, and approve it when asked. The transmitter only transmits for few seconds once every 5 minutes, so don't be surprised if Android doesn't list it under paired devices most of the time.

The [System Status](../../troubleshoot/systemstatus) page (top-left menu) has a "Dex Status" heading with the parameters you'll need throughout this page: transmitter ID, time since last connection, firmware version, transmitter days, and battery voltages.

#### Dexcom device compatibility

- **t:slim pump** — start the G6 sensor **on the pump**, not in xDrip, and don't also start it in xDrip. After the warm-up period xDrip will detect and pick up the running session on its own (tap **Restart Collector** on the Classic Status page if it doesn't). You don't need to enter the calibration code into xDrip either — only the transmitter needs it.
- **Dexcom receiver** — you can run it alongside xDrip, but avoid sending duplicate start/stop/calibrate commands from both. If you must use the receiver, start the sensor there and let xDrip pick up the session after warm-up. Don't stop the sensor from xDrip thinking it only affects xDrip — it stops the whole session on the transmitter.
- You **cannot** use a t:slim pump and a Dexcom receiver at the same time (they need the same transmitter slot), and you cannot use xDrip and the Dexcom app at the same time either — unless the Dexcom app is the only one collecting, in which case xDrip can follow it via the [Companion app](../companion).

#### Before you start

To start, you need a transmitter and a sensor.

!!!info "Transmitter expiry"  
    Check the two dates printed on the transmitter box: the manufacture date, and a Ship-By or Use-By date. Don't accept a transmitter whose Ship-By/Use-By date has already passed. Don't remove a transmitter from its box, or touch its contacts, until you intend to use it — an internal day counter starts the moment you do, and it cannot be stopped. The [warranty](../../troubleshoot/g6/#transmitter-warranty) is only valid if you start using the transmitter within 5 months of it being shipped to you, and the last day you can start (or restart) a sensor on a G6 transmitter is transmitter day 99.

#### Setup xDrip

!!!xdrip "<img src="../../images/hamburger_menu.png" style="zoom:75%;" />"  
    &ensp;Settings  
    &emsp;Hardware Data Source  
    &ensp;&emsp;Dex

Then verify the settings below — most match xDrip defaults, so this is a check rather than a reconfiguration. What each setting does is described in the [Dexcom advanced settings reference](#dexcom-advanced-settings-reference), [Bluetooth Settings](../../use/bluetooth) and [Other misc options](../../use/misc) pages.

!!!xdrip "<img src="../../images/hamburger_menu.png" style="zoom:75%;" />"  
    &ensp;Settings  
    &emsp;Dex Debug Settings

!!!xdripitem "Native Algorithm <span class='symbol'><img src="../../images/EN.png" style="zoom:75%;" /></span>"  
    &ensp;Use the transmitter's internal algorithm to calculate glucose values when possible.

!!!xdripitem "Restart Sensor <span class='symbol'><img src="../../images/DIS.png" style="zoom:75%;" /></span>"  
    &ensp;Automatically restart a sensor when it stops

!!!xdripitem "Preemptive restarts <span class='symbol'><img src="../../images/DIS.png" style="zoom:75%;" /></span>"  
    &ensp;Preemptively restart a sensor before the session ends on day 6/9 to avoid the warm-up time

Keep both restart options disabled. Reusing the same sensor beyond its normal session is a separate, riskier technique — read [Restarting a sensor](../../troubleshoot/g6/#restarting-a-sensor-beyond-its-normal-session) before considering it.

!!!xdripitem "Go on without calibrations <span class='symbol'><img src="../../images/EN.png" style="zoom:75%;" /></span>"  
    &ensp;Keep using data even if there are not enough good calibrations (Experimental!)

!!!xdripitem "Minimize Scanning <span class='symbol'><img src="../../images/DIS.png" style="zoom:75%;" /></span>"  
    &ensp;Use heuristics to minimize Bluetooth scanning + save power

!!!xdripitem "Avoid Scanning <span class='symbol'><img src="../../images/DIS.png" style="zoom:75%;" /></span>"  
    &ensp;Avoid scanning as much as possible (reduces battery usage further)

!!!info "Scanning options"  
    Minimize Scanning and Avoid Scanning save battery but are disastrous on some phones (Samsung, Chinese brands, ...). Start with both disabled — only try enabling them, one at a time, once your connection is reliable.

!!!xdripitem "Allow OB1 unbonding <span class='symbol'><img src="../../images/DIS.png" style="zoom:75%;" /></span>"  
    &ensp;OB1 collector can unbond if it thinks the encryption has failed. If you get problems with it unpairing then disable this option. If you then totally lose connection, make sure this is enabled.

!!!xdripitem "Special Pairing Workaround <span class='symbol'><img src="../../images/DIS.png" style="zoom:75%;" /></span>"  
    &ensp;Some Samsung devices can have an error where they lose the pairing information. This attempts to work around the issue.

Enable Special Pairing Workaround on Samsung and some Chinese phones.

!!!xdrip "<img src="../../images/hamburger_menu.png" style="zoom:75%;" />"  
    &ensp;Settings  
    &emsp;[Less common settings](../../use/lesscommon)  
    &ensp;&emsp;[Other misc options](../../use/misc)

!!!xdripitem "Engineering mode <span class='symbol'><img src="../../images/DIS.png" style="zoom:75%;" /></span>"  
    &ensp;Allows changing the most unsafe settings which could break everything!

!!!xdripitem "Battery Optimization prompt <span class='symbol'><img src="../../images/DIS.png" style="zoom:75%;" /></span>"  
    &ensp;On Android 6+ ensure battery optimization is switched off (recommended)

!!!xdripitem "Wake Workarounds <span class='symbol'><img src="../../images/EN.png" style="zoom:75%;" /></span>"  
    &ensp;Use workarounds to avoid non-standard Android behavior of some handsets. Without this, collectors usually fail to get data.

!!!xdripitem "Use Excessive Wakelocks <span class='symbol'><img src="../../images/DIS.png" style="zoom:75%;" /></span>"  
    &ensp;Older gratuitous wakelocks which can drain extra battery but might be needed for Nightwatch or Android Wear.

!!!xdripitem "Display Predictive Values <span class='symbol'><img src="../../images/DIS.png" style="zoom:75%;" /></span>"  
    &ensp;Old method - Not recommended!

!!!xdrip "<img src="../../images/hamburger_menu.png" style="zoom:75%;" />"  
    &ensp;Settings  
    &emsp;[Less common settings](../../use/lesscommon)

!!!xdripitem "Aggressive service restarts <span class='symbol'><img src="../../images/EN.png" style="zoom:75%;" /></span>"  
    &ensp;Repeatedly restart the collection service at any hint of missing data. Enable only if you are getting data loss.

!!!xdrip "<img src="../../images/hamburger_menu.png" style="zoom:75%;" />"  
    &ensp;Settings  
    &emsp;[Less common settings](../../use/lesscommon)  
    &ensp;&emsp;[Advanced Calibration](../../calibrate/advancedcal)

!!!xdripitem "Automatic Calibration <span class='symbol'><img src="../../images/DIS.png" style="zoom:75%;" /></span>"  
    &ensp;Calibrate using new blood glucose readings if the conditions appear right to do so without asking confirmation (experimental)

!!!xdrip "<img src="../../images/hamburger_menu.png" style="zoom:75%;" />"  
    &ensp;Settings  
    &emsp;[Less common settings](../../use/lesscommon)  
    &ensp;&emsp;[Bluetooth Settings](../../use/bluetooth)

!!!xdripitem "Turn Bluetooth on <span class='symbol'><img src="../../images/EN.png" style="zoom:75%;" /></span>"  
    &ensp;Automatically enable Bluetooth if it is switched off when we try to connect to a Bluetooth device

!!!xdripitem "Bluetooth Watchdog <span class='symbol'><img src="../../images/EN.png" style="zoom:75%;" /></span>"  
    &ensp;Reset Bluetooth by turning it off then on as a way to keep the Dex data source working. Without this, the Dex collector may fail.

!!!xdripitem "Close GATT on BLE disconnect <span class='symbol'><img src="../../images/DIS.png" style="zoom:75%;" /></span>"  
    &ensp;If the Bluetooth watchdog activates too often then you can try unchecking this option to see if it helps

!!!xdripitem "Use scanning <span class='symbol'><img src="../../images/DIS.png" style="zoom:75%;" /></span>"  
    &ensp;Scan before connecting on xBridge and Libre Bluetooth

!!!xdripitem "Use BLE manager <span class='symbol'><img src="../../images/DIS.png" style="zoom:75%;" /></span>"  
    &ensp;Use a more modern BLE manager system

!!!xdripitem "Trust Auto-Connect <span class='symbol'><img src="../../images/DIS.png" style="zoom:75%;" /></span>"  
    &ensp;Use and trust Android Bluetooth auto-connect feature

Start with Trust Auto-Connect disabled. Try enabling it only if you keep losing the connection.

!!!xdripitem "Use Background Scans <span class='symbol'><img src="../../images/EN.png" style="zoom:75%;" /></span>"  
    &ensp;Use Android 8+ background scanning feature

!!!xdripitem "Companion Bluetooth <span class='symbol'><img src="../../images/DIS.png" style="zoom:75%;" /></span>"  
    &ensp;Use Bluetooth with Companion app data source

!!!xdripitem "Bluetooth Wakelocks <span class='symbol'><img src="../../images/DIS.png" style="zoom:75%;" /></span>"  
    &ensp;Older Bluetooth wakelocks which can drain battery but might be needed for Bluetooth reception.

!!!xdripitem "xBridge+ Polling Mode <span class='symbol'><img src="../../images/DIS.png" style="zoom:75%;" /></span>"  
    &ensp;Experimental support for xBridge+ polling feature

#### Setting up for the first time

!!!warning "Please be patient"  
    Do not rush to tap "Start Sensor" even if xDrip encourages you to. Follow the steps below in order to avoid the most common issues.

!!!xdrip "<img src="../../images/hamburger_menu.png" style="zoom:75%;" />"  
    &ensp;Settings  
    &emsp;Hardware Data Source  
    &ensp;&emsp;Dex

1. If you're using a brand-new transmitter, take note of its serial number, printed on the transmitter and on its box. It activates as soon as you snap it into a sensor — you'll need this number for up to 110 days. If the serial number ends in `-1`, use only the first six characters.
2. Every sensor has a 4-digit calibration code printed on its adhesive cover — you'll need it to start the session. Don't open the sensor container until you're ready to insert it, and don't throw away the adhesive cover before you've noted the code.
3. If there's a command stuck in the queue on the Dex Status page, [clear the queue](../../troubleshoot/g6/#transmitter-queue-stuck) before continuing.
4. If "Start Sensor" isn't offered as an option from the top-left menu, tap "Stop Sensor" instead and wait 5 minutes.
5. The transmitter must be detached from your body for at least 10 minutes before you proceed — [removing it without damaging the sensor](../../troubleshoot/g6/#removing-a-transmitter-without-damaging-the-sensor) explains how, if you're moving it from a previous sensor.
6. If you're activating a new transmitter, enter its serial number as the [Dexcom Transmitter ID](#dexcom-transmitter-id).
7. Clean the transmitter contacts with rubbing alcohol (skip this if you're restarting the same sensor) and let them dry for about 10 seconds. Snap the transmitter into the sensor — this is what activates a brand-new transmitter.
8. Insert the sensor following the manufacturer's applicator instructions. Keep dust and blood away from the contacts.
9. Watch the [System Status](../../troubleshoot/systemstatus) Dex Status page — it updates every 5 minutes. Don't proceed if you don't have proper connectivity; sort that out first. If you're activating a new transmitter, be patient and wait for the battery voltages to appear before continuing — it can take up to half an hour.
10. Tap Start Sensor and enter the 4-digit calibration code from the sensor's adhesive cover when asked. Keep the code — you may need it again later.

!!!warning "Never soak a sensor"  
    Never soak (insert a sensor a while before starting it in xDrip). Factory calibration is the main advantage of G6/ONE over G5, and soaking interferes with it. The longer the gap between inserting the sensor and starting it, the more you undermine that accuracy.

Readings normally start about 2 hours after starting the sensor, with no initial calibration needed. If you're coming from G5 or another CGM, give factory calibration a chance — most people don't need to calibrate a G6/ONE sensor at all as long as the calibration code was entered correctly.

#### Starting a subsequent sensor

When one sensor's session ends, a single transmitter can be moved on to a new sensor and reused — up to around 11 sensors over its lifetime.

1. Tap Stop Sensor from the top-left menu (even if the sensor has already stopped and "Start Sensor" isn't offered yet). Wait for "stop sensor" to clear the queue on the Dex Status page — this normally takes about 5 minutes.
2. [Remove the old transmitter without damaging the sensor](../../troubleshoot/g6/#removing-a-transmitter-without-damaging-the-sensor) it's currently in, so you can keep using the sensor's adhesive site, or simply peel off the old sensor if you're done with it.
3. Follow [Setting up for the first time](#setting-up-for-the-first-time) again from step 5 onward, using the new sensor's own calibration code.

!!!info  
    Reusing the *same* sensor for longer than its normal session by tricking the transmitter into restarting it is a separate, riskier technique with a real risk of causing incorrect high readings — see [Restarting a sensor](../../troubleshoot/g6/#restarting-a-sensor-beyond-its-normal-session) in troubleshooting before attempting it.

#### Calibration

See [Calibration basics](../../calibrate/101) and [Calibrate](../../calibrate/calibrate) for what calibration is and when you should do it. You shouldn't need to calibrate a G6/ONE sensor at all under normal factory calibration — over-calibrating is discouraged by the manufacturer too.

If you do need to calibrate, tap "Add Calibration" from the top-left menu. This only becomes available after at least 3 readings have come in following a sensor start or restart. Only calibrate when the trend is flat or close to flat, and never enter a calibration that corrects the current reading by more than 20%. If you need a bigger correction, space consecutive calibrations at least 25 minutes apart (45 minutes before a third one) rather than doing it in one large step — otherwise you risk a confused calibration error that can stop readings for hours with no way to know how long the recovery will take.

#### Problems?

Try the [G6, ONE troubleshooting](../../troubleshoot/g6/) page.

</br>

---

### Dexcom advanced settings reference

!!!xdrip "<img src="../../images/hamburger_menu.png" style="zoom:75%;" />"  
    &ensp;Settings  
    &emsp;Dexcom Transmitter ID  
    &emsp;Dex Debug Settings

Reference documentation is [here](https://navid200.github.io/xDrip/docs/Dexcom_page.html).

</br>

#### Dexcom Transmitter ID

Tells xDrip which transmitter to scan for, authenticate against, and associate incoming BLE traffic with.

*Collector: Dexcom collectors and Dex-related bridge paths — Dexcom G5/G6/G7 and some Dex bridge setups*

!!!xdrip "<img src="../../images/hamburger_menu.png" style="zoom:75%;" />"  
    &ensp;Settings  
    &emsp;Dexcom Transmitter ID

!!!xdripitem "Dexcom Transmitter ID"  
    &ensp;Enter the transmitter serial number (first six characters only if it ends in `-1`)

#### Native Algorithm

Uses the transmitter's own glucose/eGlucose request path when supported, instead of the raw sensor-data path.

*Collector: OB1 Dex collector (`Ob1G5CollectionService`) — Dexcom G5/G6/G7*

!!!xdripitem "Native Algorithm"

#### Minimize Scanning

Reduces scan frequency using timing and known-device heuristics to save battery.

*Collector: OB1 Dex collector (`Ob1G5CollectionService`) — Dexcom G5/G6/G7*

!!!xdripitem "Minimize Scanning"

#### Avoid Scanning

Pushes scan reduction further, relying more heavily on previously known transmitter timing and identity.

*Collector: OB1 Dex collector (`Ob1G5CollectionService`) — Dexcom G5/G6/G7*

!!!xdripitem "Avoid Scanning"

#### Allow OB1 unbonding

Allows xDrip to remove and rebuild the BLE bond when Dexcom pairing or encryption gets into a bad state.

*Collector: OB1 Dex collector (`Ob1G5CollectionService`) — Dexcom G5/G6/G7*

!!!xdripitem "Allow OB1 unbonding"

#### Special pairing workaround

Changes the OB1 pairing/connection sequence to work around device-specific bonding problems.

*Collector: OB1 Dex collector (`Ob1G5CollectionService`) — Dexcom G5/G6/G7, especially troublesome phone models*

!!!xdripitem "Special pairing workaround"

#### Specified Slot (engineering)

Overrides the authentication slot used in the Dexcom auth flow, for engineering/troubleshooting cases.

*Collector: OB1 Dex collector (`Ob1G5CollectionService`) — Dexcom G5/G6/G7*

!!!xdripitem "Specified Slot"

</br>

[*Last modified 6/9/2026*](https://github.com/NightscoutFoundation/xDrip/releases/tag/2026.09.04)
